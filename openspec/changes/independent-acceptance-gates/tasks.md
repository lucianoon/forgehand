## 1. Acceptance contract

- [x] 1.1 Add bounded operator suite and persisted evidence models, preserving legacy profile fingerprints.
- [x] 1.2 Pin case identities and criterion coverage during selection; reject uncovered criteria and policy drift.

## 2. Execution and enforcement

- [x] 2.1 Run read-only, network-disabled acceptance containers and compare raw captured output on the host, retaining cleanup/cancellation safeguards.
- [x] 2.2 Integrate evidence veto, actionable feedback and scoped review summaries into the graph/publication path.

## 3. Verification and handoff

- [x] 3.1 Verify model validation, false-green builds, missing/mismatched evidence, retry feedback and real Python/Node Docker behavior; run regression/lint/types.
- [x] 3.2 Document activation, an executable example and limits; validate OpenSpec and record results.
