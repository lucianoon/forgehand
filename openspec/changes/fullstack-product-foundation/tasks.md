## 1. Persistent runtime

- [x] 1.1 Add portable transactional storage, PostgreSQL pooling, migration and model compatibility checks.
- [x] 1.2 Add provisioned users, password hashing, revocable sessions, origin protection and shared login throttling.
- [x] 1.3 Add validated owner-scoped CRUD, pagination, export and optimistic concurrency.

## 2. Product delivery

- [x] 2.1 Build authenticated browser workspace with persistent CRUD, visible failure states and responsive layout.
- [x] 2.2 Export independent versioned packages and expose authenticated download in the studio.
- [x] 2.3 Include pinned runtime dependencies, non-root Docker/Compose and operational setup/backup guidance.

## 3. Verification

- [x] 3.1 Verify SQLite and PostgreSQL persistence, auth, isolation, throttling, validation and conflicts.
- [x] 3.2 Exercise exported application in browser and document measured results and remaining production gaps.
